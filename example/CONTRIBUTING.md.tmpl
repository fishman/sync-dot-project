# Contributing

This project lives at `Project-HAMi/.project`. Replace the placeholder string
above (default `Project-HAMi/.project`) with your own in the action's
`placeholder` input; it is replaced with `{org}/{repo}` per repo.

Thanks for contributing!
