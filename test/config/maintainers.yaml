# Roster the harness diffs against the real kubernetes/kubernetes OWNERS.
# Kubernetes uses a filter-based OWNERS, so our flat roster surfaces as additions.

maintainers:
  - project_id: "kubernetes"
    org: "kubernetes"
    teams:
      - name: "approvers"
        members:
          - alice-approver
          - bob-approver
      - name: "reviewers"
        members:
          - carol-reviewer
